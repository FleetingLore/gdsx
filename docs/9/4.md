## 一元函数与多元函数复合的情形

> `定理1` 如果函数 $u = \varphi (t)$ 及 $v = \psi (t)$ 都在点 $t$ 可导,函数 $z = f(u, v)$ 在对应点 $(u, v)$ 具有连续偏导数,那么复合函数 $z = f \left[\varphi (t), \psi (t)\right]$ 在点 $t$ 可导,且有
> 
> $$
\frac{\text{d} z}{\text{d}t}
= \frac{\partial z}{\partial u} \frac{\text{d} u}{\text{d} t}
+ \frac{\partial z}{\partial v} \frac{\text{d} v}{\text{d} t}
\tag{4-1}
$$

> `表述`
> 
> 可把定理推广到复合函数的中间变量多余两个的情形.

`名词` 全导数

## 多元函数与多元函数复合的情形

> `定理` 如果函数 $u = \varphi (x, y)$ 及 $v = \psi (x, y)$ 都在点 $(x, y)$ 具有对 $x$ 及对 $y$ 的偏导数,函数 $z = f (u, v)$ 在对应点 $(u, v)$ 具有连续偏导数,那么复合函数 $z = f\left[\varphi(x, y), \psi (x, y)\right]$ 在点 $(x, y)$ 的两个偏导数都存在,且有
> 
> $$
\begin{matrix}
\frac{\partial z}{\partial x} = \frac{\partial z}{\partial u} \frac{\partial u}{\partial x} + \frac{\partial z}{\partial v} \frac{\partial v}{\partial x}
\tag{4-3}
\frac{\partial z}{\partial y} = \frac{\partial z}{\partial u} \frac{\partial u}{\partial y} + \frac{\partial z}{\partial v} \frac{\partial v}{\partial y}
\tag{4-4}
\end{matrix}
$$
> 
> 三元以及多元情况同理.

## 其他情形

> `定理` 
> 
> 如果函数 $u = \varphi (x, y)$ 在点 $(x, y)$ 具有对 $x$ 及对 $y$ 的偏导数,函数 $v = \psi (y)$ 在点 $y$ 可导,函数 $z = f(u, v)$ 在对应点 $(u, v)$ 具有连续偏导数,那么复合函数 $z = f\left[\varphi (x, y), \psi (y)\right]$ 在点 $(x, y)$ 的两个偏导数都存在,且有
> 
> $$
\begin{matrix}
\frac{\partial z}{\partial x} = \frac{\prtial z}{\partial u} \frac{\partial u}{\partial x}
\tag{4-7}
\frac{\partial z}{\partial y} = \frac{\partial z}{\partial u} \frac{\text{d} u}{\text{d} y} + \frac{\partial z}{\partial v} \frac{\text{d} v}{\text{d} y}
\tag{4-8}
\end{matrix}
$$

## 全微分形式不变性
